import React from 'react';

import MenuItem from './components/MenuItem';

import './style.css';

const Menu = () => (
  <div
    className="menu__container"
  >
    <MenuItem item={{}} />
    <MenuItem item={{}} />
    <MenuItem item={{}} />
    <MenuItem item={{}} />
    <MenuItem item={{}} />
    <MenuItem item={{}} />
  </div>
);

export default Menu;
